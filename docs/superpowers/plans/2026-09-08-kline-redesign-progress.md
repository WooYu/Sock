# K 线重设计阶段进展（2026-09-08）

## 本次收口范围

为降低长会话中断风险，本节点只推送已经存在于 Git 提交中、且可以重新验证的功能。未形成持久提交的实现不计入完成。

## 已完成并纳入分支

1. 预测与绘图数据契约
   - 未来三日预测快照结构。
   - 基于“时间 + 价格”的绘图坐标。
   - 旧工作区数据校验与迁移保护。
2. 图表计算与坐标转换
   - K 线、MA、BOLL 与图表坐标计算拆分。
   - 响应式 SVG 坐标转换与留白修正。
3. 后端未来三日基线预测接口
   - 日线预测快照与 MA/BOLL 延伸。
   - 周线、月线明确返回不支持错误。
4. 绘图引擎
   - 创建、选中、控制点编辑、整图拖动、锁定、显示、删除、撤销与重做。
   - 交易日索引拖动，避免跨周末后图形漂移或消失。
5. 图表预测层与绘图层
   - 真实行情与未来推演区分。
   - 预测区仍可继续绘图，预测图形只读。
   - 趋势线、矩形、水平线、文字及买入/卖出/目标/止损标记命中区域。
   - 指针捕获与取消清理；零位移不产生历史记录。
   - 鼠标选中后自动聚焦图表，支持 Delete、Esc、撤销与重做快捷键。

## 尚未完成

1. 预测值查看面板与对象属性检查器。
2. 按股票和周期隔离的 V2 持久化、350ms 防抖、离线队列与冲突恢复。
3. 最终桌面/移动端视觉还原。
4. Playwright 工作流、桌面/移动截图对比与最终验收。

## 后续建议拆分

后续每次只实施一个任务并立即提交、推送：

1. Task 6：预测值面板与对象属性检查器。
2. Task 7：持久化、同步与冲突恢复。
3. Task 8：响应式视觉。
4. Task 9：浏览器与视觉验收。

每个任务完成后都应记录提交哈希和验证命令，避免依赖会话内的临时状态。
