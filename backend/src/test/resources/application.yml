spring:
  datasource:
    url: jdbc:h2:mem:stockcal;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false
  flyway:
    enabled: false
